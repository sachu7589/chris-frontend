{
  "buildCommand": "npx vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps && chmod +x node_modules/.bin/* 2>/dev/null || true",
  "framework": "vite",
  "nodeVersion": "18.x",
  "env": {
    "NODE_ENV": "production"
  },
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}
